import React from 'react';
import {Grid} from "@mui/material";

const Home = () => {
    return (
        <>
            <Grid container spacing={2}>
                home page
            </Grid>
        </>
    );
};

export default Home;